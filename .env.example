# Configuration example for cursor++
# Copy this file to .env and modify as needed

# Application name (default: cursor++)
# APP_NAME=cursor++

# Directory name used for cursor rules (default: .cursor/rules)
# RULES_DIR_NAME=.cursor/rules

# Registry file name (default: registry.json)
# REGISTRY_FILE_NAME=registry.json

# Directory permission in octal (default: 0755)
# DIR_PERMISSION=0755

# File permission in octal (default: 0644)
# FILE_PERMISSION=0644

# Log level (default: info)
# Available levels: trace, debug, info, warn, error, fatal, panic
# LOG_LEVEL=info

# Log file name (default: cursor++.log)
# LOG_FILE_NAME=cursor++.log

# GitHub Token for releases
# Create a Personal Access Token with 'repo' scope at:
# https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Other environment variables can be added below 